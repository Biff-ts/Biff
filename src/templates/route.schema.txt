import { z } from "zod";

export const {{name}}Schema = {
  input: z.object({
    title: z.string()
  }),
  output: z.object({
    id: z.string(),
    title: z.string()
  })
};
export const PostInput = z.object({
  title: z.string(),
  body: z.string(),
});

export type {{Name}}Input = z.infer<typeof {{name}}Schema.input>;
export type {{Name}}Output = z.infer<typeof {{name}}Schema.output>;
